# ==============================================================================
# PROJECT MIRAGE — DEPENDENCY & ENVIRONMENT REQUIREMENTS
# Supported Platforms: macOS (Apple Silicon / Intel) & Windows 10/11 (x64)
# ==============================================================================

# Core Rust Cryptographic Engine:
# -------------------------------
# Rust >= 1.80.0 (cargo, rustc)
# Installation:
#   - macOS / Linux: curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
#   - Windows: https://win.rustup.rs/

# Universal Python Launcher (No external pip libraries needed - uses Standard Library):
# -------------------------------------------------------------------------------------
# Python >= 3.8

# Optional Web Legacy Environment:
# --------------------------------
# Node.js >= 18.0.0
# npm >= 9.0.0

# ==============================================================================
# QUICKSTART / CÓMO EJECUTAR (WINDOWS Y MACOS)
# ==============================================================================

# 1. Ejecución Automática Universal con Python (macOS y Windows):
# --------------------------------------------------------------
# python run.py              # Inicia la interfaz gráfica
# python run.py --test       # Ejecuta Known Answer Tests (KAT)
# python run.py --cli --help # Usa el CLI en terminal

# 2. Ejecución Nativa Directa:
# ---------------------------
# macOS:
#   ./mirage                 (o 'cargo run --release')
#
# Windows (PowerShell o CMD):
#   .\mirage.bat             (o '.\mirage.ps1' o 'cargo run --release')
